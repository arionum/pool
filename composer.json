{
    "require": {
        "rain/raintpl": "^3.1"
    },
    "require-dev": {
        "squizlabs/php_codesnifferr": "^3.2"
    },
    "scripts": {
        "check-style": "phpcs -p -l --standard=PSR2 --extensions=php .",
        "fix-style": "phpcbf -p -l --standard=PSR2 --extensions=php ."
    }
}
